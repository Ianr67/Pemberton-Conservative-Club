# Public URL of the shared API. This value is safe to expose to the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3002/api/v1
# Server-only URL used by the admin authentication proxy.
API_BASE_URL=http://localhost:3002/api/v1
