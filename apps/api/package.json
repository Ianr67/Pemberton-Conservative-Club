{
  "name": "@pcc/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "nest start --watch",
    "start": "node dist/main.js",
    "test": "vitest run --pool=threads --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pcc/contracts": "workspace:*",
    "@pcc/database": "workspace:*",
    "@pcc/validation": "workspace:*",
    "@nestjs/common": "^12.0.1",
    "@nestjs/core": "^12.0.1",
    "@nestjs/platform-express": "^12.0.1",
    "pg": "^8.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^12.0.0",
    "@types/express": "^5.0.0",
    "@types/pg": "^8.0.0"
  }
}
